import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AddActionPage';

export default defineMessages({
  title: {
    id: `${scope}.title`,
    defaultMessage: 'Add new action',
  },
});
